[build-system]
requires = [
    "setuptools>=65",
    "wheel",
    "cython>=0.29",
    "numpy"
]
build-backend = "setuptools.build_meta"

[project]
name = "msbwt-ak"
version = "0.1.0"
description = "Python 3 compatible MSBWT fork"
requires-python = ">=3.8"
dependencies = [
    "numpy",
    "pysam"
]
